# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0

receivers:
  otlp:
    protocols:
      grpc:
        endpoint: ${env:OTEL_COLLECTOR_HOST}:${env:OTEL_COLLECTOR_PORT_GRPC}
      http:
        endpoint: ${env:OTEL_COLLECTOR_HOST}:${env:OTEL_COLLECTOR_PORT_HTTP}
        cors:
          allowed_origins:
            - "http://*"
            - "https://*"
  httpcheck/frontend-proxy:
    targets:
      - endpoint: http://${env:FRONTEND_PROXY_ADDR}
  nginx:
    endpoint: http://${env:IMAGE_PROVIDER_HOST}:${env:IMAGE_PROVIDER_PORT}/status
    collection_interval: 10s
  docker_stats:
    endpoint: unix:///var/run/docker.sock
  postgresql:
    endpoint: "postgresql:5432"
    username: "root"
    password: "otel"
    metrics:
      postgresql.blks_hit:
        enabled: true
      postgresql.blks_read:
        enabled: true
      postgresql.tup_fetched:
        enabled: true
      postgresql.tup_returned:
        enabled: true
      postgresql.tup_inserted:
        enabled: true
      postgresql.tup_updated:
        enabled: true
      postgresql.tup_deleted:
        enabled: true
      postgresql.deadlocks:
        enabled: true
    tls:
      insecure: true
  redis:
    endpoint: "valkey-cart:6379"
    username: "valkey"
    collection_interval: 10s
  # Host metrics
  hostmetrics:
    root_path: /hostfs
    scrapers:
      cpu:
        metrics:
          system.cpu.utilization:
            enabled: true
          system.cpu.logical.count:
            enabled: true
      disk:
      load:
      filesystem:
        exclude_mount_points:
          mount_points:
            - /dev/*
            - /proc/*
            - /sys/*
            - /run/k3s/containerd/*
            - /var/lib/docker/*
            - /var/lib/kubelet/*
            - /snap/*
          match_type: regexp
        exclude_fs_types:
          fs_types:
            - autofs
            - binfmt_misc
            - bpf
            - cgroup2
            - configfs
            - debugfs
            - devpts
            - devtmpfs
            - fusectl
            - hugetlbfs
            - iso9660
            - mqueue
            - nsfs
            - overlay
            - proc
            - procfs
            - pstore
            - rpc_pipefs
            - securityfs
            - selinuxfs
            - squashfs
            - sysfs
            - tracefs
          match_type: strict
      memory:
        metrics:
          system.memory.utilization:
            enabled: true
          system.memory.limit:
            enabled: true
      network:
      paging:
        metrics:
          system.paging.usage:
            enabled: true

      processes:
      process:
        mute_process_exe_error: true
        mute_process_io_error: true
        mute_process_user_error: true
      system:
        metrics:
          system.uptime:
            enabled: true
exporters:
  otlp:
    endpoint: "172.31.15.80:4317"
    tls:
      insecure: true
      insecure_skip_verify: true

  otlphttp:
    endpoint: "http://172.31.15.80:4318"
    tls:
      insecure: true
      insecure_skip_verify: true

processors:
  batch:
  memory_limiter:
    check_interval: 5s
    limit_percentage: 80
    spike_limit_percentage: 25
  resourcedetection:
    detectors: [env, docker, system]
  transform:
    error_mode: ignore
    span_statements:
      - context: span
        statements:
          - set(attributes["category"], nil) # Redaction
    # trace_statements:
    #   - context: span
    #     statements:
    #       # # could be removed when https://github.com/vercel/next.js/pull/64852 is fixed upstream
    #       - replace_pattern(name, ".*", "")
    #       # - replace_pattern(name, "\\?.*", "")
    #       # - replace_match(name, "GET /api/products/*", "[REDACTED]")
    #       # - replace_pattern(span.attributes["cloud.account.id"], ".*", "[REDACTED]") 
    # span_statements:
    #   - context: span
    #     statements:
    #       - set(attributes["category"], nil) # Redaction
    #       # - set(attributes["id"], "REDACTED") # Replacement
    #       # - set(attributes["id"], SHA256(attributes["id"])) # Hashing
    # span_statements:
    #   - context: span
    #     statements:
    #       - delete(attributes["category"])
    #       - set(attributes["id"], "REDACTED")
    #       - set(attributes["name"], hash(attributes["name"], "sha256"))
    # metric_statements:
    #   - context: datapoint
    #     statements:
    #       - delete(attributes["category"])
    #       - set(attributes["id"], "REDACTED")
    #       - set(attributes["name"], hash(attributes["name"], "sha256"))
extensions:
  health_check:
    endpoint: 0.0.0.0:13133
  pprof:
    endpoint: 0.0.0.0:1777
  zpages:
    endpoint: 0.0.0.0:55679

service:
  extensions: [health_check, pprof, zpages]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [resourcedetection, memory_limiter, transform]
      exporters: [otlp, otlphttp]
    metrics:
      receivers: [docker_stats, httpcheck/frontend-proxy, hostmetrics, nginx, otlp, postgresql, redis]
      processors: [resourcedetection, memory_limiter, batch]
      exporters: [otlp, otlphttp]
    logs:
      receivers: [otlp]
      processors: [transform]
      exporters: [otlp, otlphttp]
  telemetry:
    metrics:
      level: detailed
      readers:
        - periodic:
            interval: 10000
            timeout: 5000
            exporter:
              otlp:
                protocol: http/protobuf
                endpoint: ${env:OTEL_COLLECTOR_HOST}:${env:OTEL_COLLECTOR_PORT_HTTP}
